# Claude Starter Archive

`claude-starter` has been moved out of this repository and is no longer maintained here.

Current archive location:

- [`/workspace/claude-starter`](/workspace/claude-starter)

Why:

- the Claude-specific line grew beyond what this repository should keep carrying
- current work is focused on `codex-starter`
- when the Claude line is mature enough again, it should be restored as its own standalone project and linked back from here

Current policy:

- do not add new `claude-starter` changes in this repository
- treat the copy under `/workspace/claude-starter` as an external archive/worktree, not as part of `agent-skills`
